/**
 * @deprecated Import from "@/lib/middlewares" instead.
 * This file is kept for backward compatibility.
 */
export { requireCartSession as cartSessionMiddleware } from "@/lib/middlewares";

// cartOrAuthMiddleware was unused dead code — removed.
